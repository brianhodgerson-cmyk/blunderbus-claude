# BlunderBus Python dependencies
# Install: pip install -r requirements.txt

# SSH (paramiko avoids Windows subprocess+SSH hang bug)
paramiko>=4,<5

# HTTP requests (SecOnion Kratos auth, TrueNAS, Obsidian REST API)
requests>=2.31,<3

# ClickHouse native protocol (finance data on Cortex)
clickhouse-driver>=0.2.9,<0.3

# Monarch Money API (finance ingest)
monarchmoney
gql[aiohttp]>=3,<4  # monarchmoney 0.1.x is not compatible with gql 4.x execute_async API

# Discord/Hermes alerts and legacy Telegram bot dependencies
python-telegram-bot>=22,<23

# Redis (Telegram conversation history persistence)
redis>=5,<6

# BlunderBus registry/API models
pydantic>=2.13,<3
PyYAML>=6,<7
psycopg[binary]>=3.3,<4
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
fastmcp>=3.2,<4

# Timezone support
pytz>=2024.1
tzlocal>=5,<6

# Google OAuth / API clients (calendar and fitness flows)
google-api-python-client>=2,<3
google-auth>=2,<3
google-auth-httplib2>=0.2,<1
google-auth-oauthlib>=1.2,<2
